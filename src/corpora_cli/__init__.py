"""corpora-cli — the ``corpora`` command-line surface.

The pipeline itself (parsers, Text-Fabric conversion, storage) lives in the
corpora-py distribution; this package only renders it as a Rich-styled,
scriptable CLI.
"""
